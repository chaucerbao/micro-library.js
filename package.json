{
  "name": "micro-library.js",
  "version": "0.0.1",
  "description": "A set of JavaScript libraries for the minimalist",
  "repository": {
    "type": "git",
    "url": "https://github.com/chaucerbao/micro-library.js"
  },
  "scripts": {
    "start": "./node_modules/.bin/browser-sync start --files \"src/**/*.js, test/**/*.js\" --no-open --no-inject-changes --no-online --server --startPath \"test/browser.html\""
  },
  "devDependencies": {
    "babel": "*",
    "browser-sync": "*",
    "chai": "*",
    "es6-module-loader": "*",
    "mocha": "*",
    "sinon": "*"
  }
}
